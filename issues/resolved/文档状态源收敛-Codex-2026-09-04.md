# 根目录文档状态源收敛

**报告者**: Codex
**日期**: 2026-09-04
**优先级**: Medium
**类型**: Documentation
**状态**: Resolved

## 问题

仓库根目录存在多份一次性会话/装配报告，其中包含“完整64点Q8.8 FFT已解锁”、
“16x16/Q8.8硬件库完成”、“8-100倍实测”和“所有任务完成”等结论。后续真实
RTL复验已经证明这些表述不成立或仅是未经benchmark的估算。继续保留多份根目录
报告会让临时回报与`issues/`中的当前状态互相冲突。

## 处理

删除以下tracked临时报告：

- `ASSEMBLY_WORK_REPORT_2026-09-03.md`；
- `FINAL_ASSEMBLY_REPORT_2026-09-03.md`；
- `SESSION_SUMMARY.md`。

同时删除根目录零字节、由VCS生成且已被`.gitignore`忽略的`ucli.key`。
Verdi/DVE启动时产生的`DVEfiles/`、`vfastLog/`和`novas.rc`也从根目录
删除；需要保留的VCS KDB、VPD、转换后VCD及Verdi运行日志均位于
ignored的`build/vcs_fft64_vsp/`中。
`README.md`是项目入口，继续保留；`.claude/`为本地配置且已忽略，不属于文档清理范围。

删除的tracked文件仍可从Git历史恢复，不另建`archived/`副本，避免把过期结论换一个
目录继续传播。

## 事实归并位置

- FFT微码、RAM/cache、VCS实测和Verdi波形状态：
  `issues/open/FFT微码RTL闭环执行缺口-Codex-2026-09-03.md`；
- 数学库8-bit已验证边界及16-bit/Q8.8阻塞：
  `issues/in-progress/数学库硬件加速-Claude-2026-09-03.md`；
- 可执行静态BFP8 FFT数值与内存契约：`docs/workloads/fft64-q7.md`；
- 项目入口与验证命令：`README.md`。

`issues/README.md`也已明确：`open/`和`in-progress/`共同构成活跃问题，
`resolved/`只保存已完成收口且有验证证据的事项。

## 一致性修复

- `docs/delivery/MATH_HARDWARE_ACCEL_IMPL.md`增加复验更正，不再把汇编通过
  等同于16-bit/Q8.8 RTL正确或性能实测；
- `docs/delivery/MEMORY_BASED_ROUTING_STRATEGY.md`把旧FFT勾选降为历史评估，
  指向当前静态BFP8闭环；
- 三个受阻示例中已失效的`BLOCKER_FIX_REPORT_2026-09-03.md`引用改为当前FFT issue。
- 删除ignored的`build/algorithms/math_fp16_add.{hex,lst,json}`陈旧产物；
  该概念稿已从验证构建排除，新的`math_bfp8_static_scale`build产物保留。

## 验收

- 根目录仅保留项目级`README.md`和构建入口，不再保留一次性报告；
- FFT和数学库各只有一个明确的issue状态源；
- 删除清单、原因、恢复方式和当前权威文档均可追溯。
