# Minimal PC-to-cluster EXEC program.  The two vector actions are consumed in
# program order and the exact final word C0000000 retires the ordered stream.

entry:
    EXEC_ALU_RR op=xor mode=byte va=1 vb=1 vd=1
    EXEC_ALU_RI op=add mode=byte va=1 vd=2 imm=7
program_end:
    CONTROL_END
